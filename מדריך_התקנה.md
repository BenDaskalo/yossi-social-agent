# מדריך התקנה — יוסי סושיאל אייג'נט 🎬

---

## מה תצטרך לפני שמתחילים

- מחשב Mac או Windows
- חיבור לאינטרנט
- מפתח Anthropic API (מקבלים בשלב 3)

---

## שלב 1 — הורד את הסוכן

פתח דפדפן ועבור לקישור הזה:

👉 **https://github.com/BenDaskalo/yossi-social-agent/archive/refs/heads/main.zip**

יורד קובץ ZIP — שמור אותו בשולחן העבודה.

לחץ פעמיים על הקובץ כדי לפתוח אותו.
תקבל תיקייה בשם `yossi-social-agent-main` — העבר אותה לשולחן העבודה.

---

## שלב 2 — התקן Python

פתח דפדפן ועבור ל: **https://www.python.org/downloads/**

לחץ על הכפתור הגדול "Download Python".
הפעל את ההתקנה ולחץ Next עד הסוף.

> אם Python כבר מותקן אצלך — דלג לשלב הבא.

---

## שלב 3 — קבל מפתח Anthropic

עבור ל: **https://console.anthropic.com/**

- הירשם (חינם)
- לחץ על **API Keys** בתפריט השמאלי
- לחץ **Create Key**
- תן לו שם (לדוגמה: "יוסי-סוכן")
- **העתק את המפתח** — הוא נראה כך: `sk-ant-...`

> שמור אותו — לא תוכל לראות אותו שוב.

---

## שלב 4 — הכנס את המפתח לסוכן

פתח את התיקייה `yossi-social-agent-main`.

מצא קובץ בשם `.env.example`.

**Mac:** לחץ ימני → פתח עם → TextEdit
**Windows:** לחץ ימני → פתח עם → Notepad

תראה שורה כזו:
```
ANTHROPIC_API_KEY=your_anthropic_api_key_here
```

מחק את הטקסט `your_anthropic_api_key_here` והדבק את המפתח שלך.

שמור את הקובץ **בשם חדש**: `.env` (בלי הסיומת `.example`)

הקובץ צריך להיראות כך:
```
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxx
APIFY_API_KEY=apify_api_xxxxxxxxxxxxxx
CLAUDE_MODEL=claude-sonnet-4-6
```

> מפתח ה-Apify כבר מוכן בקובץ — לא צריך לשנות אותו.

---

## שלב 5 — הרץ את ההתקנה

**Mac:**
1. פתח Terminal (חפש "Terminal" ב-Spotlight)
2. הקלד:
```bash
cd ~/Desktop/yossi-social-agent-main
bash install.sh
```
3. המתן עד שרואים ✅ ההתקנה הושלמה

**Windows:**
1. פתח Command Prompt (חפש "cmd" בתפריט Start)
2. הקלד:
```
cd %USERPROFILE%\Desktop\yossi-social-agent-main
pip install -r requirements.txt
```

---

## שלב 6 — הפעל את הסוכן

**Mac:**
```bash
python3 agent.py
```

**Windows:**
```
python agent.py
```

תראה את המסך הזה — הסוכן מוכן:
```
========================================
   🎬 יוסי סושיאל אייג'נט
   מנהל תוכן אישי עם זיכרון ולמידה עצמית
========================================
```

---

## איך משתמשים

### לכתוב תסריט
```
יוסי > סרטון למה עסק בלי אתר מפסיד לקוחות
```

### לסרוק מתחרה
```
יוסי > סרוק @username
```

### לאשר תסריט (הסוכן לומד ממנו)
```
יוסי > אשר 1
```

### לשפר תסריט
```
יוסי > שפר 1
```
הסוכן ישאל אותך מה לשנות — תגיד לו בעברית פשוטה.

### לצאת
```
יוסי > יציאה
```

---

## חשוב לדעת

- **הסוכן זוכר הכל** — כל תיקון שאתה נותן, הוא שומר ולא חוזר על הטעות
- **הסרטונים נשמרים** בתיקייה `scripts/` בתוך הפרויקט
- **כל שיחה מלמדת** את הסוכן להכיר אותך טוב יותר

---

## בעיות נפוצות

**"python3: command not found"**
→ חזור לשלב 2 והתקן Python מחדש

**"ModuleNotFoundError"**
→ הרץ שוב: `pip3 install -r requirements.txt`

**"Invalid API Key"**
→ בדוק שהמפתח ב-.env מתחיל ב-`sk-ant-` וללא רווחים

---

**שאלות? פנה לבן ישירות.**
